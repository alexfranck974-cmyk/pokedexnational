# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v54.0.0/ before writing any code.

Note: this project was scaffolded on SDK 57 (latest as of 2026-07) but downgraded to SDK 54 because Expo Go on the iOS App Store only supports up to SDK 54. Reupgrade when Expo Go catches up.
